[package]
name = "inwdeck-pricing"
version = "0.1.0"
edition = "2021"
description = "Provider pricing and cost calculation for inwdeck"

[dependencies]
serde = { version = "1", features = ["derive"] }
serde_json = "1"
