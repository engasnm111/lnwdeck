[package]
name = "lnwdeck-hook-manager"
version = "0.2.0"
edition = "2021"
description = "Safe hook configuration manager with preview, backup and rollback"

[dependencies]
serde = { version = "1", features = ["derive"] }
serde_json = "1"
sha2 = "0.10"
hex = "0.4"
thiserror = "2"

[dev-dependencies]
tempfile = "3"
