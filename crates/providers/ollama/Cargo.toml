[package]
name = "inwdeck-provider-ollama"
version = "0.1.0"
edition = "2021"
description = "Ollama local LLM provider adapter for inwdeck"

[dependencies]
inwdeck-domain = { path = "../../domain" }
inwdeck-provider-runtime = { path = "../../provider-runtime" }
chrono = { version = "0.4" }
