[package]
name = "inwdeck-provider-copilot"
version = "0.1.0"
edition = "2021"
description = "GitHub Copilot provider adapter for inwdeck"

[dependencies]
inwdeck-domain = { path = "../../domain" }
inwdeck-provider-runtime = { path = "../../provider-runtime" }
chrono = { version = "0.4" }
