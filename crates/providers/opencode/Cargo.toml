[package]
name = "inwdeck-provider-opencode"
version = "0.1.0"
edition = "2021"
description = "OpenCode CLI provider adapter for inwdeck"

[dependencies]
inwdeck-domain = { path = "../../domain" }
inwdeck-provider-runtime = { path = "../../provider-runtime" }
chrono = { version = "0.4" }
