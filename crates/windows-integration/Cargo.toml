[package]
name = "inwdeck-windows-integration"
version = "0.1.0"
edition = "2021"
description = "Windows-specific integration services for inwdeck"

[dependencies]
serde = { version = "1", features = ["derive"] }
serde_json = "1"
